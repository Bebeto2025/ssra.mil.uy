import Section from '../components/Section'
import Contact from '../components/Contact'

export default function ContactoPage() {
  return (
    <Section
      id="contacto"
      title="Contacto"
      subtitle="Canales oficiales del SSRA"
      variant="alt"
    >
      <Contact />
    </Section>
  )
}
